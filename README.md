# wordle-cli

Create my own Wordle!

## How to run

Install the following:

- Docker

### Build the app

```shell
docker build -t wordle-cli ./app
# or
make build
```

### Let's play

```shell
docker run --rm -it wordle-cli
# or
make
```
